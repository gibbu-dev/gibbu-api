spring:
  # Test DB config
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
    driver-class-name: org.h2.Driver

  # Spring data JPA config
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

